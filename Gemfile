# TODO: determine exact versions that work
#if RUBY_VERSION.to_f < 2
#  raise "Unsupported Ruby Version: #{RUBY_VERSION} (2.0+ required)"
#end

source "https://rubygems.org"
gemspec # read gem requirements from binproxy.gemspec
